# Hardware libs are optional and platform-specific. Install what you need.
# These are listed here for reference, not pinned, and don't install on dev
# machines. Use `pip install -r requirements-pi.txt` on the Pi itself.
